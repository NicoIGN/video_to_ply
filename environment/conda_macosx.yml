name: gsplat
channels:
  - conda-forge

dependencies:
  - python=3.11
  - pip
  - rclone
  - tree

  # 🔧 build
  - cmake
  - ninja

  # 🧮 math stable
  - numpy
  - scipy
  - eigen=3
  - suitesparse

  # 🖼️ vision
  - openimageio
  - ffmpeg

  # 🧊 geometry / SfM stack
  - "colmap>=3.8,<4.0"
  - pycolmap>=0.6
  - cgal
  - faiss-cpu

  # 🖥️ UI
  - glew
  - qt=5

  # system
  - libiconv

  - pip:
      # 🔥 ML stack stable
      - torch
      - torchvision

      # nerfstudio stable
      - nerfstudio==1.1.5

      # COLMAP bindings correct
      - pycolmap

      # utils
      - imageio
      - imageio-ffmpeg
